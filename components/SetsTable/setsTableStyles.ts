import { StyleSheet } from 'react-native';

export const setsTableStyles = StyleSheet.create({
  shadow: {
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.3,
    // shadowRadius: 3.84,
  },
  setWidth: {
    width: 62,
    textAlign: 'center',
    // borderRadius: 6,
    // alignContent: 'center',
    // borderWidth: 2,
    // borderColor: 'blue',
  },
  weightWidth: {
    width: 62,
    textAlign: 'center',
    // borderRadius: 6,
    // borderWidth: 2,
    // borderColor: 'blue',
  },
  repsWidth: {
    width: 62,
    textAlign: 'center',
    // borderRadius: 6,
    // borderWidth: 2,
    // borderColor: 'blue',
  },
  infoFontSize: {
    fontSize: 18,
  },
  headerFontSize: {
    fontSize: 16,
  },
});
